function getSum( a,b )
{ // let arr = []
  sum = 0
  if (a == b) {
   return a;
  } else if (a < b) {
   for (i = a; i <= b; i++) {
   sum += i;
   };
   return sum;
  } else {
   for (i = b; i <= a; i++) {
   sum += i;
   };
   return sum;
  };
}
